sudo: require
language: go
go:
  - 1.6
services:
  - docker
  - redis-server
  - mongodb
notifications:
  email:
    - jeffhann@gmail.com
before_install:
  - npm install
  - docker build -t ftd/api .
  - docker run -d -p 3000:3000 ftd/api 
before_script:
  - mongorestore
script:
  - ./test 
